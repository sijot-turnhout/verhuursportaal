<?php

declare(strict_types=1);

namespace Tests\Feature\Filament\Clusters\PropertyManagement\Resources\Changelog\Actions;

test ('if an admin has the right permission to close an changelog', function (): void {
});

test ('if an boarding member has the right permission to close an changelog', function (): void {
});

test ('if the user who is respnsible for the changelog can close the changelog', function (): void {
});

test ('if we cannot close a changelog that has already been closed', function (): void {
});

test ('If we can successfully close a changelog with the action', function (): void {
});
