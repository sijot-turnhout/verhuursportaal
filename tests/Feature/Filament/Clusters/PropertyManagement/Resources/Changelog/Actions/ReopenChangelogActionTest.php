<?php

declare(strict_types=1);

namespace Tests\Feature\Filament\Clusters\PropertyManagement\Resources\Changelog\Actions;

test('if an admin has the right permission to reopen an changelog', function (): void {});

test('if an boarding member has the right permission to reopen changelogs', function (): void {});

test('if the user is responsible for the changelog van reopen the changelog if needed', function (): void {});

test('if we cannot reopen the changelog when it has already the open state', function (): void {});

test('if we can successfully reopen a changelog with the action', function (): void {});
